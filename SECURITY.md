# Security

Fail closed on duplicate receipt consumption, duplicate authorization use, altered previous hash, altered anchor, wrong-predecessor revisions, failed receipts, stage transitions masquerading as revisions, stale lifecycle anchors, missing authority evidence, conflicting lineage, replayed appends, malformed hashes/timestamps, authority claims, lifecycle-mutation claims, unknown fields or tampering.

Report issues via the repository issue tracker with the `security` label. Synthetic fixtures only; never file real production digests.
